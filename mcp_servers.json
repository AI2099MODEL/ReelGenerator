{
  "mcpServers": {
    "stitch": {
      "command": "npx",
      "args": [
        "-y",
        "@_davideast/stitch-mcp@latest"
      ],
      "env": {
        "STITCH_API_TOKEN": "${STITCH_API_TOKEN}"
      }
    }
  }
}
