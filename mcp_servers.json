{
  "mcpServers": {
    "stitch": {
      "command": "npx",
      "args": [
        "-y",
        "@google/stitch-mcp@latest"
      ],
      "env": {
        "STITCH_API_TOKEN": "${STITCH_API_TOKEN}"
      }
    }
  }
}
